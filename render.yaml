services:
  - type: web
    name: glowuprphelper
    runtime: node
    plan: free
    buildCommand: "echo ready"
    startCommand: node server.mjs
    envVars:
      - key: PORT
        value: "8787"
      - key: DOWNLOAD_FILE_URL
        sync: false
